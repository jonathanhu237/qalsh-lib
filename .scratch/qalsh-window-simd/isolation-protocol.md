# Authorized runtime CPU isolation — Stage B verification

User explicitly approved temporary CPU isolation with services left running and original settings restored afterward. This is one predeclared Stage-B verification in a materially different environment, not sample selection from prior contaminated batches. Production candidate, audited baselines, quality policy,12 triplets/six orders,30 populations and all statistical/contamination thresholds remain unchanged.

Host checks: Centaurus, cgroup v2, systemd259, noninteractive sudo available; CPU6 and18 share physical core6. Reserve this physical core for a transient benchmark slice. Temporarily constrain system.slice and user.slice to0-5,7-17,19-23. Do not stop/restart services or change governors/SMT/IRQ affinity. PID1/kernel work is not moved; its residual effects are measured, so isolation is not a claim of a perfectly idle machine. Services temporarily have11 of12 physical cores.

Before mutation save original AllowedCPUs and EffectiveCPUs. Install a root-owned independent10-minute systemd rollback timer BEFORE restricting CPU masks. The benchmark is a separate transient service under a top-level slice, with AllowedCPUs6,18 and per-run taskset6. RuntimeMaxSec480 and control-group cleanup prevent straggling benchmark children. Normal completion/failure restores masks; SSH signal handling attempts cleanup; watchdog restores independently if the controller dies. Verify restored property/effective masks, then cancel watchdog. Avoid overwriting concurrent unexpected property changes; report conflicts loudly.

Run exactly one batch tagged stageb-isolated-20260913. All36 results retained. Before/after binary/data/harness hashes must match existing provenance. Only if contamination<=20% AND both all-sample paired95% intervals are wholly below1 does Stage B pass. No retry until a lucky result. Further Stage-C measurement is contingent on this outcome and must have its own bounded isolation window; this authorization does not silently broaden into stopping services or reserving additional cores.
